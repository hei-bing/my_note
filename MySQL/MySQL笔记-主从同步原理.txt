主从同步原理
- 在mysql_master上更新数据，写入二进制日志（Binary log）,并通知存储引擎提交事务
- mysql_slave启用I/O线程将master的binary log复制到slave的中继日志（Relay log），
- mysql_slave启用SQL线程从Relay log中读取事件，并重放其中的事件从而更新slave数据，使其与master中的数据一致。
只要该线程与I/O线程保持一致，中继日志通常会位于os缓存中，所以中继日志的开销很小。